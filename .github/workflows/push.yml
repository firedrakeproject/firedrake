name: Deploy after push

on:
  push:
    branches:
      - master
      - release

jobs:
  test:
    uses: ./.github/workflows/build.yml
    with:
      target_branch: ${{ github.ref_name }}
      deploy_website: true
    secrets: inherit
